/**
 * Standard Schema validation of fetched items (PLAN §5.12).
 * @module
 */

export {
  type InvalidItem,
  normalizeIssues,
  parseWith,
  type ValidationIssue,
  validateItems,
} from './validate.ts';
