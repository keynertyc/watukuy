{
  "name": "@watukuy/example-bullmq-sink",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "bullmqSink(): every change becomes a BullMQ job (jobId = event.id) consumed by a Worker. Needs Redis.",
  "scripts": {
    "start": "node main.ts"
  },
  "dependencies": {
    "bullmq": "^6.3.7",
    "watukuy": "workspace:*"
  }
}
