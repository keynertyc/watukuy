{
  "name": "@watukuy/example-nestjs-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "NestJS 12 app: WatukuyModule, @OnWatukuyEvent handler, Terminus /health, /inspect controller.",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "pnpm build && node dist/nestjs-app/src/main.js"
  },
  "dependencies": {
    "@nestjs/common": "^12.0.3",
    "@nestjs/core": "^12.0.3",
    "@nestjs/platform-express": "^12.0.3",
    "@nestjs/terminus": "^12.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "watukuy": "workspace:*",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
