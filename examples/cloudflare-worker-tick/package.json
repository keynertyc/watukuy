{
  "name": "@watukuy/example-cloudflare-worker-tick",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "engine.tick() from a Cloudflare Workers cron trigger against a public JSON API, MemoryStore.",
  "scripts": {
    "start": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "watukuy": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "wrangler": "^4.135.0"
  }
}
